# Favicon

**Priority:** nice-to-have

## Goal

A favicon displays correctly in the browser tab when running the app.

## Scope

- Source or create a small icon appropriate for the project (album grid theme)
- Add to `public/` assets
- Wire into `index.html` head (`<link rel="icon" ...>`)
